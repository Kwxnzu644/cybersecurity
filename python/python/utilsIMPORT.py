import utils

square = int(input("enter a number to be squares ? "))
print(utils.square(square))

greet = input("what is your name ? ")
print(utils.greet(greet))

a = int(input("enter the first number ? "))
b = int(input("enter the second number ? "))
print(utils.add(a, b))